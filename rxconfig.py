import reflex as rx

config = rx.Config(
    app_name="Ticks_Tock",  # Must match the folder/filename, e.g., "your_app"
    api_url="https://{your-project}.up.railway.app/api",  # Or use environment var for flexibility
)
